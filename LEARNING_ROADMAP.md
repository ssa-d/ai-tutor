# 从 Python 基础到 AI Agent —— 学习路线图（基于 AI Tutor 项目）

> 你是**智能科学与技术专业**学生，Python 刚入门。
> 目标：把 Python 核心练扎实，然后一步步把 AI Tutor 从一个"单次问答 API"
> 升级成真正的 **AI Agent（智能体）**。

---

## 为什么这条路线是对的？

Agent（智能体）本质是：**AI 有了"工具 + 记忆 + 自主规划 + 循环执行"的能力**。
而"会调 DeepSeek API"只占其中一小部分。下面的 Python 概念，几乎在写每个 agent
功能时都会碰到：

| Agent 概念 | 依赖的 Python 核心 |
|-----------|-------------------|
| 给 AI 挂"工具"（如查天气、读文件） | 函数、类、装饰器、类型注解 |
| 多轮对话、状态管理 | 面向对象（类）、数据结构 |
| 流式输出、边想边写 | 生成器 `yield`、异步 `async` |
| 同时请求多个工具/并行任务 | 异步编程 `asyncio` |
| 结构化的返回（JSON 格式） | Pydantic 模型、异常处理 |
| 稳健处理 API 报错/重试 | 异常处理 `try/except` |

所以分两大赛区：**第一赛区把 Python 核心打牢（第 1–7 课），第二赛区进军 Agent（第 8–13 课）**。

---

# 第一赛区：Python 核心（第 1–7 课）

> 目标不是"读完"，是"每课都动手写、并在 AI Tutor 上用一次"。

## 第 1 课：数据与流程 —— 把地基夯实
- 变量、类型：`int / float / str / bool / list / dict / tuple / set`
- 条件 `if/elif/else`、循环 `for/while`
- 推导式：`[x for x in ...]`、字典推导式（写 agent 超级常用）
- 字符串格式化：`f"{name}"`

**动手**：写一个小函数 `summarize_messages(messages)`，把多轮对话消息压缩成一行摘要；
或用推导式过滤出所有 `role == "user"` 的消息。

## 第 2 课：函数 —— 把逻辑装进"盒子"
- 定义、参数（默认值、`*args`、`**kwargs`）、返回值
- 作用域、`lambda`、`map/filter`
- 你已经见过：`llm.py` 里的 `ask()` 函数

**动手**：给 `llm.py` 加一个 `chat(messages)` 函数，接受**多轮消息列表**而不是单句
问题——这是往"多轮对话"走的第一步。

## 第 3 课：面向对象（OOP）—— 理解"类"
- `class`、`__init__`、`self`、方法、属性
- 你已经见过：`app/main.py` 里的 `ChatRequest`、`ChatResponse`（Pydantic 模型就是类）
- 再看一眼 `OpenAI(...)` 也是一个"对象"，`.chat.completions.create(...)` 是它的"方法"

**动手**：建一个 `app/memory.py`，写一个 `ChatMemory` 类，能用 `add()`/`get_history()`
保存对话历史。这直接为后面"多轮 agent"铺路。

## 第 4 课：异常处理 —— 让程序不轻易崩
- `try / except / else / finally`
- 你已经见过：`app/main.py` 里的 `except Exception` 捕获 DeepSeek 调用失败
- 自定义异常、什么时候该抛

**动手**：给 `llm.py` 的 `ask()` 加**重试逻辑**：API 暂不可用时自动重试 3 次，
并在最后失败时抛出一个你自己定义的 `LLMError` 异常。

## 第 5 课：生成器与迭代器 —— 为"流式"做准备
- `yield`、`for` 的底层（`iter/next`）
- 生成器表达式
- 为什么需要它：agent 的"边推理边吐文字"就是流式

**动手**：给 `llm.py` 加一个 `ask_stream()` 函数，用 `yield` 逐块返回模型回答
（OpenAI 客户端天然支持 `stream=True`）。这是阶段 3 流式聊天的雏形。

## 第 6 课：异步编程 async —— 高性能的关键
- `async def`、`await`、`asyncio`、事件循环
- **为什么 agent 必须懂**：同时调用多个工具、等多个 API 返回时，同步会卡死
- FastAPI 天然支持 async 路由

**动手**：把 `app/main.py` 的 `/api/chat` 路由改成 `async def`，并用
`asyncio` 并发调用两个不同工具（比如同时查"天气"和"日历"）——感受什么叫不阻塞。

## 第 7 课：模块、包与工程化 —— 组织你的代码
- `import` 的本质、包 `__init__.py`
- 你已经见过：`app/` 是一个包，`from . import config, llm`
- 类型注解 `-> str`、`docstring`、常用工具

**动手**：重构——把 AI Tutor 拆出更清晰的模块（`chat/`、`tools/`、`memory/`），
为成为真正的项目打好结构。**恭喜，第一赛区通关**

---

# 第二赛区：进军 AI Agent（第 8–13 课）

> 先明确：Agent = **模型 + 工具 + 记忆 + 规划循环（thought → action → observe）**。
> 我们要亲手把一个"会说话的 API"变成"会做事、会自己规划"的智能体。

## 第 8 课：真正的"多轮对话" —— 从无状态到有状态
- 为什么现在的 `ask(question)` 是"失忆"的（每次都不记得之前聊过）
- 把第 3 课的 `ChatMemory` 用起来：把所有历史消息一起发给模型
- 理解 system / user / assistant 三种 role 的作用

**动手**：让 AI Tutor 记住你今天问过啥，回答时能"接上文"。这是 agent 的"记忆"雏形。

## 第 9 课：结构化输出 —— 让 AI 返回规范数据
- 用 Pydantic 定义希望模型返回的结构（如 `{tool: "weather", args: {...}}`）
- OpenAI / DeepSeek 支持的 `response_format`（JSON output）
- 你已会：`ChatResponse` 模型——现在用它约束更复杂结构

**动手**：让模型在想要调用工具时，返回一个标准的 JSON，而不是随心所欲的文本。
这是"工具调用"的开关。

## 第 10 课：Function Calling —— 给 AI 装上"手"
- 定义"工具描述"（名字+参数 schema 给模型看）
- 模型判断"该用哪个工具、传什么参数"，返回调用意图
- 程序真正执行这个工具，把结果回传，模型再回答

**动手**：给 AI Tutor 挂两个真工具：`get_time()`（当前时间）和 `search_web()`（网页搜索）。
当用户问"现在几点""帮我查资料"时，AI 会**真的去调用**再回答，不再乱编。

## 第 11 课：Agent 循环（ReAct）—— 让 AI 自己思考步骤
- 理解 **thought（想）→ action（做）→ observation（看结果）→ 再想** 的循环
- 用一个 `while` 循环，让模型一次次决定"要不要再调工具"，直到它认为可以回答了
- 设置**最大轮数上限**，防止死循环

**动手**：写一个 `Agent` 类：有工具、有记忆、有循环上限。问它一个需要查好几个信息
的问题（比如"今天和昨天哪个热"），看它自己一步步查、自己规划回答。

## 第 12 课：流式对话体验 —— 高手的标志
- 把第 5 课的 `ask_stream()` 接入前端，让回答"一个字一个字蹦出来"
- 结合 async，让 agent 在思考时也能给用户反馈

**动手**：让 AI Tutor 网页端实现打字机式输出，同时 agent 每"想了一步"都显示出来
（像 ChatGPT 的 reasoning 那样）。体验瞬间专业起来。

## 第 13 课：把它做成一个完整作品 + 测试
- 用 `pytest` 给记忆、工具调用、解析逻辑写测试
- 完善前端、错误处理、日志
- 部署考虑：上线到服务器 / Docker
- 更新 README、写一篇阶段 3 总结

**动手**：把你第 3 阶段的成果推上 GitHub，配上动图/截图和说明。
**这就是你找实习 / 面试作品集里最好的项目。**

---

# 路线总览（一图流）

```
第1-7课   夯实 Python 核心          （每次都在 AI Tutor 上练）
   │
   ▼
第8课   多轮对话  ──► 记忆
第9课   结构化输出 ──► 让 AI 说规范话
第10课  Function Calling ──► 给 AI 装手
第11课  ReAct 循环   ──► 让 AI 自己规划
第12课  流式+async   ──► 体验拉满
第13课  测试+部署+作品 ──► 求职作品集
```

---

# 怎么跟我（陪练）一起学？

你可以用下面任意一种方式约课，我会：
- **讲概念**（用大白话 + 代码例子）
- **带你动手**（一起改 AI Tutor，改完直接能跑）
- **出小练习**（做完我给你 review）
- **答疑问**（卡在哪都行）

**建议节奏**：一周 1~2 课，每课 30~60 分钟动手。
太赶容易放弃，太松容易断——保持手感最重要。

---

## 下次课开始前，建议先做

1. 复习一下第 1 课的 `list/dict` 和推导式（后面天天用）
2. 打开 `app/llm.py` 和 `app/main.py`，自己把里面有 `函数`、`类`、`try/except`
   的地方指出来（对应第 2/3/4 课）——指不出也没关系，我们到时候一起看

准备好了就跟我说 **"开始第 1 课"**，我们就从整理 `summarize_messages` 动手
