# 🚀 AI Tutor 开始使用·完整教程

> 面向第一次跑这个项目的你。全程尽量用"双击脚本"代替命令行，一步步来。
> 你只需要做 **4 件事**：①拿到 API Key → ②填 Key → ③双击启动 → ④双击推送到 GitHub。

---

## 你需要准备的东西

| 需要 | 说明 | 有吗？ |
|------|------|--------|
| 电脑上的 Python | 你的电脑已有 Python 3.13 | ✅ |
| DeepSeek API Key | 到 https://platform.deepseek.com 注册并创建 | ❓（第 1 步搞定） |
| GitHub 账号 | 到 https://github.com 注册 | ❓（第 4 步用） |

---

## 第 1 步：拿到 DeepSeek API Key（5 分钟，只需一次）

1. 打开浏览器，进 **https://platform.deepseek.com**
2. 手机号/邮箱注册并登录
3. 左侧菜单找 **「API Keys」**（或「API 密钥」）
4. 点 **「创建 API Key」**，起个名字（如 `ai-tutor`）
5. 复制生成的一串字符，形如：
   ```
   sk-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
   ```
   > ⚠️ 这个值只显示一次，复制好保存起来。丢了就在列表里删掉重建。

---

## 第 2 步：把你的 Key 填进 `.env`（2 分钟）

1. 打开文件夹 **`E:\新建文件夹\ai-tutor`**
2. 找到 **`.env.example`**，把它**重命名**为 **`.env`**
   （在资源管理器里：右键 → 重命名 → 改成 `.env`。如果系统提示，选"仍要"）
3. 用**记事本**打开 `.env`
4. 找到这一行：
   ```
   DEEPSEEK_API_KEY=sk-你的真实密钥填这里
   ```
   把 `sk-你的真实密钥填这里` 整段替换成你第 1 步复制的真实 Key
5. **保存并关闭**

> 📌 `.env` 里有你的密钥，**永远不要**提交到 GitHub。`.gitignore` 已经帮你挡住了。

---

## 第 3 步：双击启动（双击 start.bat）

1. 回到项目文件夹，找到 **`start.bat`**
2. **双击它**
3. 按提示等待：
   - 第一次会自动创建虚拟环境、安装依赖（可能要等 1~3 分钟）
   - 装好后会自动启动服务
4. 看到类似下面这行，就成功了：
   ```
   Uvicorn running on http://127.0.0.1:8000
   ```
5. 打开浏览器，输入 **`http://127.0.0.1:8000`**，能看见"欢迎使用 AI Tutor"就对了
6. 想测试提问：在浏览器打开 **`http://127.0.0.1:8000/docs`**，点开 `/api/chat` → Try it out → 在 message 里输入问题 → Execute，能看到 DeepSeek 的回答

> 想停止服务：在启动的黑色窗口里按 **Ctrl + C**。

---

## 第 4 步：一键推送到 GitHub（双击 push.bat）

### 4a. 先在网页上建好空仓库（一次性）
1. 登录 github.com
2. 右上角 **+** → **New repository**
3. 仓库名填 **`ai-tutor`**
4. **不要**勾选 "Initialize this repository with a README"
5. 点 **Create repository** 创建
6. 创建成功后，页面会显示一个**仓库地址**，形如：
   ```
   https://github.com/你的用户名/ai-tutor.git
   ```
   复制它（后面会用到）。

### 4b. 双击 push.bat
1. 回到项目文件夹，**双击 `push.bat`**
2. 脚本会自动：
   - 初始化 git（第一次）
   - 提示你粘贴远程仓库地址（把 4a 复制的地址粘进来再回车）
   - 提交所有改动
   - 推送到 GitHub
3. 结束后刷新你的 GitHub 页面，就能看到代码上去了

> 第一次 push 时如果弹出登录窗口，用 GitHub 账号登录即可。

---

## 我想自己用命令行，怎么做？

如果你想亲手敲命令（强烈推荐，能提升能力），核心就这几条：

```bash
# 1. 进入项目目录
cd /d E:\新建文件夹\ai-tutor

# 2. 建虚拟环境
python -m venv .venv

# 3. 激活虚拟环境（每次开新窗口都要激活）
.venv\Scripts\activate

# 4. 装依赖
pip install -r requirements.txt -i https://pypi.tuna.tsinghua.edu.cn/simple

# 5. 启动
python main.py
```

提交推送：
```bash
git init
git add .
git commit -m "阶段1"
git remote add origin 你的仓库地址
git push -u origin main
```

---

## 常见问题（FAQ）

**Q：双击 start.bat 报错"python 不是内部或外部命令"？**
你的系统没把 Python 加进 PATH。可改用 `py -m venv .venv`，或在安装 Python 时勾选 "Add Python to PATH"。

**Q：装依赖很慢 / 失败？**
脚本已默认用清华镜像。如果还失败，检查网络；或把 `-i https://pypi.tuna.tsinghua.edu.cn/simple` 换成官方源。

**Q：/api/chat 报错"服务端尚未配置 DEEPSEEK_API_KEY"？**
说明 `.env` 没填对。重新打开 `.env` 确认 Key 格式是 `sk-xxx` 且没有多余空格。

**Q：推送到 GitHub 失败？**
最常见是：还没配置远程仓库、或没登录。重跑 `push.bat`，或在命令行里手动 `git remote add origin 你的地址` 后重试。

---

## 你现在学到了什么 ✅

- ✅ 什么是虚拟环境（venv），为什么要隔离
- ✅ 环境变量 / `.env` 管理密钥的安全习惯
- ✅ 用 `requirements.txt` 管理依赖
- ✅ FastAPI 的 `/`、`/docs`、`/api/chat`
- ✅ git + GitHub 的提交推送流程

---

下一阶段预告：让 AI 读取你上传的 PDF 并总结/回答——正好能和你之前那个"PDF 作业填写"结合起来练习。
