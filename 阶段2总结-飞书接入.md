# 🚀 阶段 2 总结：AI 接入飞书机器人（从 0 到跑通）

> 日期：今天
> 目标：让 AI Tutor 不再只是网页 API，而是**住进飞书、能跟你在聊天里对话**的机器人。
> 结果：✅ 在飞书里给机器人发消息，能收到 DeepSeek 的 AI 回答。

---

## 一、为什么做这一步

原来的 AI Tutor 只是"网页端一问一答"（阶段 1）。你想把它做成**私人 AI 生涯助手**，
第一步就是**有一个自然的聊天入口**——飞书。这一步实现了"在飞书里跟 AI 对话"，
是后续所有功能（记忆、DDL 提醒、规划、求职推荐）的**入口地基**。

---

## 二、实现原理（看懂这张图就够了）

```
你在飞书里给机器人发消息
        │
        ▼
飞书开放平台（云端）—— 收到你的消息
        │  (通过 WebSocket 长连接推送事件)
        ▼
你的电脑跑的 run_feishu.py（lark-oapi 机器人）
        │  (解析消息 → 调用 DeepSeek → 生成回答)
        ▼
飞书 IM API 把回复发回给你的会话
        │
        ▼
你在飞书里收到 AI 的回答
```

**关键技术选择**：用 **WebSocket 长连接** 接收消息，
**不需要公网 IP、不需要内网穿透**（最适合个人开发）。

---

## 三、完整步骤记录

### 1. 在飞书开放平台建应用
- 网址：<https://open.feishu.cn>
- 登录 → 「创建企业自建应用」→ 填应用名（如 `AI Tutor`）
- 进入应用后台 → 「添加能力」→ 添加 **机器人**

### 2. 拿到并配置凭证
- 后台左侧 **「凭证与基础信息」** → `App ID` 和 `App Secret`
- 填入项目 `.env`（**绝不提交 GitHub**）：
  ```ini
  FEISHU_APP_ID=cli_xxxxxxxxxxxx
  FEISHU_APP_SECRET=xxxxxxxxxxxxxxxxxxxxxxxx
  ```

### 3. 配置「事件订阅」——接收消息
- 左侧 **「事件与回调」→「事件订阅」**
- 添加事件：**接收消息 `im.message.receive_v1`**（版本 v2.0）
- 开通权限：**"读取用户发给机器人的单聊消息"**

### 4. 编写代码（`app/feishu.py`）
核心三步：
1. `EventDispatcherHandler` 注册 `im.message.receive_v1` 事件
2. 收到消息 → `json.loads(content)` 取出文本 → 调 `llm.ask()`
3. 用 `ReplyMessageRequest` 把回答回复回去

### 5. 启动并测试
- 项目根目录：`.venv\Scripts\python.exe run_feishu.py`
- 看到 `connected to wss://msg-frontier.feishu.cn/...` 即连接成功
- 在飞书里发消息，能收到 AI 回答 ✅

---

## 四、实战踩坑记录（这些最重要！）

### 坑 1：遇到 emoji 打印崩溃
- 现象：DeepSeek 回答里带 emoji（😊），Windows 黑窗口（GBK 编码）打印时报错
  `UnicodeEncodeError: 'gbk' codec can't encode character '\U0001f60a'`
- 原因：控制台默认 GBK，无法编码未知字符，`print` 崩溃打断流程
- 解决：`run_feishu.py` 里给 stdout/stderr 设 UTF-8 + `errors="replace"`：
  ```python
  sys.stdout.reconfigure(encoding="utf-8", errors="replace")
  ```
- **教训**：Windows 上处理含 emoji/特殊字符的字符串，要强制 UTF-8。

### 坑 2：多个机器人实例竞争
- 现象：开了多个 `run_feishu.py` 进程同时连同一个应用，飞书事件随机推给某一个，
  你看的窗口日志和别人收到的回复对不上。
- 解决：**只保留一个实例**，其余全 `Stop-Process` 杀掉。
- **教训**：WebSocket 长连接同名应用不要多开实例。

### 坑 3：最坑的——「已发送回复」但对方收不到
- 现象：日志打印 `[DEBUG] 已发送回复`，但飞书里就是收不到。
- 排查：写独立脚本 `test_send.py` 直接调 IM API 发消息，打印 `resp.code`
- 真相：**返回错误码 `99991672`**：
  > Access denied. ...required scope: `im:message:send` / `im:message:send_as_bot`
  > 应用尚未开通所需的应用身份权限
- 原因：只开了「读取」消息权限，**没开「发送消息」权限**。
  而 lark-oapi 的 `.reply()` 调用**不抛异常**，只在返回 code 里报错，
  我们的代码没检查 code，所以误以为"已发送"。
- 解决：到飞书后台「权限管理」开通 **`im:message:send_as_bot`** → **重新发布应用** → 重启机器人。
- **超重要教训**：
  1. 飞书 API 的失败**在返回的 code 里，不在异常里**，一定要检查 `resp.code == 0`！
  2. 事件订阅和权限开通后，**必须重新发布应用**才生效。
  3. 排查问题要**打印 API 真实返回**，不要只相信自己的假设。

---

## 五、你真正学会的技能

| 类别 | 掌握点 |
|------|--------|
| 飞书开放平台 | 建应用、开能力、权限管理、版本发布 |
| 事件订阅 | `im.message.receive_v1`、订阅+发布生效机制 |
| 长连接 | WebSocket 免公网 IP 接收消息 |
| lark-oapi SDK | 事件分发器、IM 收发消息、Client 构建 |
| 调试能力 | 看日志、打印 API code、权限诊断（99991672）|
| 编码处理 | Windows GBK vs UTF-8，emoji 崩溃修复 |
| git 流程 | 提交推送、确认 .env 不误传、忽略日志 |

---

## 六、当前能力 vs 下一步

### 现在能做的
- ✅ 在飞书里跟 AI 对话（一问一答）
- ✅ 用 `start_feishu.bat` 一键启动机器人
- ✅ 阶段性：收到消息 → DeepSeek → 回复全链路

### 还没有的（下一步目标）
- ❌ **记忆**：AI 不记得之前聊过什么（每次都是单句）
- ❌ 总结每日、规划日程
- ❌ Canvas DDL 提醒
- ❌ 动态简历、求职推荐、3D 可视化

**下一步推荐**：让 AI 有记忆（多轮对话）——把 `summarize_messages` 和对话历史用起来，
这正好衔接"第 2 课(函数)+第 8 课(多轮对话)"。

---

## 七、快速启动命令（备用）

```cmd
cd /d E:\新建文件夹\ai-tutor
set PYTHONIOENCODING=utf-8
.venv\Scripts\python.exe run_feishu.py
```
或直接双击 `start_feishu.bat`。

> 保持黑窗口开着 = 机器人在线。Ctrl+C 停止。
